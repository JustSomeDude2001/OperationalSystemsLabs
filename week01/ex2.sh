# Note that the output file was moved to
# the .sh file's directory for ease of use

mkdir ~/week01
cd ~/week01

history > ex2.txt

# NOTE: history seems to be callable only from terminal manually.
# Hence, I added history manually by calling "history > ex2.txt" from terminal